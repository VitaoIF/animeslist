services:
  postgres:
    image: postgres:16
    container_name: animelist-db
    environment:
      POSTGRES_DB: animelist
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"